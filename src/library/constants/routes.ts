export type RoutesType = Array<[string, string, string]>;

const routes: RoutesType = [
  ['Detail', '/detail', 'mdi:card-text-outline'],
  ['Tech Stack', '/tech-stack', 'mdi:integrated-circuit'],
  ['Project', '/project', 'mdi:package-variant-closed'],
  ['Experience', '/experience', 'mdi:briefcase']
];

export default routes;
